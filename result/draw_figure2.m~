%% Draw error decreasing figure for bp 

filename = 'readDatabp(0.5l0.0m100i).txt';
data = importdata(filename);
num = length(data) - 1;
err = data(1:end-1);

figure, clf, 
plot(1:num, )
grid on, gird minor, 